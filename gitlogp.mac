// git log -p ファイル名

$$git = "git.exe log -p ";
$$cmd = $$git + filename2;
$$dir = directory2 + "\\";
$$file = getenv("TMP") + "\\" + basename2 + "logp" + "." + filetype;
runex $$cmd,0,0,"",2,$$file,0,"",2,$$dir,0,0,encode;
openfile "/r " + $$file;
endmacro;
