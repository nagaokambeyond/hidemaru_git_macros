if (selecting == 1){
  // git blame -L startlineno, endlinenoファイル名
  $git = "git.exe blame -L " + str(seltopy) + "," + str(selendy) + " ";
}else{
  // git blame ファイル名
  $git = "git.exe blame ";
}

$$cmd = $git + filename2;
$$dir = directory2 + "\\";
runex $$cmd,0,0,"",7,"",0,"",2,$$dir,0,0,6;
endmacro;
